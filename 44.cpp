#include<bits/stdc++.h>
using namespace std;


int main()
{
    int n;
    cin>>n;
    int a[n];
    int b[0]];
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
    }
    for(int i=0;i<n;i++)
    {
        for(int j=0;j<n;j++)
        {
            if(a[i]<a[j+1])
            {
               b[0]=
            }
        }
    }
//     int X=*min_element(a, a + n);
//     X=a[i]=*min_element(a, a + n);
// cout<<a[i]<<endl;

    return 0;
}